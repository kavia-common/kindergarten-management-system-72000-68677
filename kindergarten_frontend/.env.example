# Environment configuration for kindergarten_frontend

# MODE can be 'local' (uses SQLite) or 'prod' (uses HTTP API)
MODE=local

# API base URL for production mode (ignored in local mode)
API_BASE_URL=https://api.example.com

# Optional: WebSocket URL for real-time messaging/notifications in prod
WS_URL=wss://api.example.com/ws
